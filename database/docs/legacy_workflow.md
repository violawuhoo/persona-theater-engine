# Legacy Workflow Notes

The previous persona-markdown input flow (`/database/personas/*.md`) is deprecated.

Legacy sources are archived in:

- `/database/legacy/persona_sources/`

Current active workflow is seed-first via `/database/archetypes/*.md`.
